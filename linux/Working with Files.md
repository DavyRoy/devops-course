📁 Working with Files — Работа с файлами

4.1 File Permissions — Права доступа

Подтемы:
•	Типы прав:
•	r — чтение
•	w — запись
•	x — выполнение
•	Категории пользователей:
•	u — владелец
•	g — группа
•	o — остальные
•	Проверка прав:
•	ls -l
•	Изменение прав:
•	chmod 755 file (цифры)
•	chmod u+x file (символы)
•	Изменение владельца/группы:
•	chown user file
•	chgrp group file
•	SUID, SGID, sticky bit (кратко)

⸻

4.2 Archiving and Compressing — Архивация и сжатие

Подтемы:
•	Архивы через tar:
•	Создать: tar -cvf archive.tar dir/
•	Разархивировать: tar -xvf archive.tar
•	Работать с tar.gz: tar -czvf, tar -xzvf
•	Сжатие:
•	gzip file
•	gunzip file.gz
•	zip -r archive.zip dir
•	unzip archive.zip
•	Отличие “архивация” vs “сжатие”
•	Частые форматы: .tar, .gz, .bz2, .xz, .zip

⸻

4.3 Copying and Renaming — Копирование и переименование

Подтемы:
•	Копирование файлов:
•	cp file1 file2
•	Копирование директорий:
•	cp -r dir1 dir2
•	Копирование с подтверждением/перезаписью:
•	cp -i, cp -f
•	Переименование:
•	mv old new
•	Перемещение в другую директорию:
•	mv file dir/
•	Маски в копировании (*.txt)

⸻

4.4 Soft Links / Hard Links — Символические и жёсткие ссылки

Подтемы:
•	Что такое ссылки:
•	Жёсткая ссылка — указывает на inode
•	Символическая ссылка — как ярлык
•	Создание ссылок:
•	Жёсткая: ln file linkname
•	Символическая: ln -s file linkname
•	Проверка ссылок:
•	ls -l показывает стрелку →
•	Когда использовать:
•	Soft — для директорий, разных дисков
•	Hard — только внутри одной FS
•	Удаление ссылок не удаляет оригинал