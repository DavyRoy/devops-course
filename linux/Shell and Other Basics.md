🐚 Shell and Other Basics — Основы Shell и сопутствующее

3.1 Command Path — Путь к командам (PATH)

Подтемы:
•	Что такое переменная PATH
•	Где хранятся исполняемые файлы (/bin, /usr/bin, /sbin, /usr/local/bin)
•	Проверка пути к команде:
•	which command
•	whereis command
•	Добавление пути в PATH:
•	export PATH=$PATH:/new/path
•	Конфигурационные файлы (.bashrc, .profile)

⸻

3.2 Environment Variables — Переменные окружения

Подтемы:
•	Что такое переменные окружения
•	Просмотр всех переменных:
•	env
•	printenv
•	Создание переменных:
•	MYVAR=value
•	Экспорт переменной в среду:
•	export MYVAR=value
•	Удаление переменной:
•	unset MYVAR
•	Переменные оболочки:
•	USER
•	HOME
•	SHELL

⸻

3.3 Command Help — Помощь по командам

Подтемы:
•	man — полная документация
•	man -k — поиск по ключевым словам
•	--help — краткая помощь
•	Чтение синтаксиса команд
•	Разделы man pages (1 — команды, 5 — конфигурации, 8 — админские)

⸻

3.4 Redirects — Перенаправления

Подтемы:
•	Стандартные потоки:
•	stdin (0)
•	stdout (1)
•	stderr (2)
•	Перенаправление вывода:
•	> — перезаписать
•	>> — добавить
•	Перенаправление ввода:
•	< file
•	Перенаправление ошибок:
•	2> — только ошибки
•	> file 2>&1 — вывод + ошибки
•	Конвейеры (pipe):
•	| — передача вывода одной команды в другую

⸻

3.5 Super User — Суперпользователь (root)

Подтемы:
•	Разница между user и root
•	sudo — выполнение команды от имени root
•	sudo su / su - — переключение на root
•	Файлы конфигурации sudoers:
•	/etc/sudoers
•	visudo
•	Привилегии и безопасность
•	Ограничение доступа через группы (sudo, wheel)