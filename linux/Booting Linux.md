🔧 12. Booting Linux — Загрузка Linux

12.1 Logs — Логи загрузки

Подтемы:
•	Основные логи загрузки:
•	journalctl -b — логи текущей загрузки
•	journalctl -b -1 — предыдущая загрузка
•	Поиск ошибок при старте:
•	journalctl -p err -b
•	Логи ядра:
•	dmesg
•	Типичные проблемы:
•	ошибки драйверов
•	сбои монтирования
•	проблемы с сервисами при старте

⸻

12.2 Boot Loaders — Загрузчики

Подтемы:

GRUB (самый распространённый загрузчик)
•	Файлы конфигурации:
•	/etc/default/grub
•	/boot/grub/grub.cfg
•	Обновление конфигурации:
•	update-grub (Debian/Ubuntu)
•	grub2-mkconfig (RHEL/CentOS)
•	Меню загрузки GRUB
•	Изменение параметров загрузки ядра (GRUB_CMDLINE_LINUX)

Загрузка ядра
•	Что такое initrd / initramfs
•	Роль ядра Linux в процессе загрузки

Процесс загрузки systemd
•	Старт PID 1 — systemd
•	Этапы:
1.	Bootloader → загружает ядро
2.	Ядро → initramfs
3.	initramfs → монтирует root
4.	Запуск systemd
5.	Запуск сервисов по зависимостям

Полезные команды
•	Проверить время загрузки:
•	systemd-analyze
•	Граф зависимостей:
•	systemd-analyze critical-chain