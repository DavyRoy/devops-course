🚀 Navigation Basics — Основы навигации

1.1 Basic Commands — Базовые команды

Подтемы:
•	pwd — показать текущую директорию
•	ls — список файлов
•	cd — переход между директориями
•	clear — очистить терминал
•	man — справка по командам
•	whoami — текущий пользователь

⸻

1.2 Moving Files / Directories — Перемещение файлов и директорий

Подтемы:
•	mv — перемещение файлов
•	mv file dir/ — переместить файл в папку
•	mv folder1 folder2/ — переместить директорию
•	Использование масок (*.txt)
•	Перемещение с заменой существующих файлов

⸻

1.3 Creating & Deleting Files / Dirs — Создание и удаление файлов/директорий

Подтемы:
Создание:
•	touch file — создать пустой файл
•	mkdir dir — создать директорию
•	mkdir -p — создать вложенные директории

Удаление:
•	rm file — удалить файл
•	rm -r dir — удалить директорию
•	rm -rf dir — принудительное удаление
•	rmdir dir — удалить пустую директорию

⸻

1.4 Directory Hierarchy Overview — Обзор иерархии директорий

Подтемы:
•	/ — корневая директория
•	/home — домашние каталоги пользователей
•	/root — дом. каталог root
•	/etc — конфигурационные файлы
•	/var — логи, временные данные
•	/usr — программы и библиотеки
•	/bin, /sbin — основные исполняемые файлы
•	/tmp — временные файлы
•	/opt — сторонние приложения
•	/dev — устройства
•	/proc — виртуальная файловая система (процессы, ядро)
